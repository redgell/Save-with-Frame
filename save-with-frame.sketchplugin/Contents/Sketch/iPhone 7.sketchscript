@import 'library/common.js'

var document;
var onRun = function(context) {
	var options = ['Jet Black', 'Matte Black', 'Gold', 'Rose Gold', 'Silver', 'Space Gray'],
		deviceStyle = createSelectDialog("Choose device type:", options)
	;


	com.redgell.scale = 2.0;
	com.redgell.context = context;
	com.redgell.export('iPhone7', context, deviceStyle[1])
}
